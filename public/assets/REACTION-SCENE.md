# Approved reaction scene

`reaction-test-bmx-approved-4k.jpg` is the previously approved 3840 × 2160 edited/upscaled scene, copied byte-for-byte. It preserves the camera, hill, concrete pad, tree, track atmosphere, BMX bikes and white boundary lines. SHA-256: `dd09e8aa896cbf3c4f4584b0e5d027acdcd30f96f4ef68cb27926e1b2006125a`.

`reaction-test-bmx-gate-reveal.jpg` is a generated hidden-background plate, used only through the local clip under the moving gate. It must never replace the complete approved master. Built-in image editing removed the raised gate to reveal the flush receiver and dirt behind it. The tool returned 1672 × 941; JPEG conversion at quality 95 did not resize it. SHA-256: `62ecdf32ab139363970f4bb96d24999f958376faf475cc031661f8bb48d39933`.

The gate texture is sampled from the approved master and follows the existing fixed hinge. Logical scene coordinates and source-image pixel dimensions are distinct. Keep all moving surfaces and the photographed lamp masks aligned in the same logical coordinate space.

The exact generation prompt, input and output paths are retained in the Playground workspace at `output/tracklab-release70/IMAGE-PROVENANCE.md`.
